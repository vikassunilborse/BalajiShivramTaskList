export enum TaskStatus {
  TODO = 'todo',
  IN_PROGRESS = 'in-progress',
  COMPLETED = 'completed',
  OVERDUE = 'overdue',
  ALL = 'All'
}

export enum TaskPriority {
  ALL = 'All',  
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high'
}
